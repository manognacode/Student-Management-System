# Student-Management-System
Student Management System is a simple full-stack app using Python (Flask), SQLite, HTML, and JavaScript. It allows users to add, view, and delete student records. The frontend communicates with the backend using REST APIs, and data is stored and retrieved dynamically from the database.
